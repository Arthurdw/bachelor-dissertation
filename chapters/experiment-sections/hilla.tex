% zelfde notes als React
% geen web framework, focused zich op de comunicatie tussen de backen den frontend, java types, validatie en endpoints worden automatish omgezet naar typescript
% generics op custom recrods werken niet
% method overloading neem een random overload (wat dus verwarrend is)
% backend volgt geen REST specification (altijd POST request, dus geen caching optimalisaties)
% its this or nothing,  forceert je om spring boot te gebruiken in combinatie met REact of Lit
% handled essentieel alle API communicatie (auth, csrf, endpoints, types) wat relatief makkelijk zelf te maken is
% heel expliciet zijn op je types (je bloat je code met @Notnull decorators)
% meegeleverde Vaadin componenten zijn gemakkelijk in gebruik

\section{Hilla}
\label{sec:hilla}

\subsection{Overview}
\label{subsec:hilla:overview}
\itodo{write this}

\subsection{Strengths}
\label{subsec:hilla:strengths}
\begin{itemize}
    \item strength! \todo{write strengths}
\end{itemize}

\subsection{Weaknesses}
\label{subsec:hilla:weaknesses}
\begin{itemize}
    \item weakness! :( \todo{write weaknesses}
\end{itemize}